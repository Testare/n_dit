//! Utility module for helping with pathfinding for AI

use crate::{Direction, Node, Point};

pub fn find_any_path_to_point(sprite_key: usize, target: Point, node: &Node) -> Vec<Direction> {
    Vec::new()
}
